<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteRule    ^$    cv/    [L]
    RewriteRule    (.*) cv/$1    [L]
 </IfModule>

#OPTIONS
Options All -Indexes

#PROTECT .HTACCESS
<Files .htaccess>
    order allow,deny
    deny from all
</Files>
